export default function ConnectionsPanel({ groups }) {
  const visibleGroups = (groups || []).map((group) => ({ ...group, items: (group.items || []).filter(Boolean) })).filter((group) => group.items.length);
  if (!visibleGroups.length) return null;
  return <div className="panel" style={{ marginTop: 18 }}><div className="panel-head"><div className="panel-title">Connections</div></div><div style={{ padding: "14px 20px 24px", display: "grid", gap: 18 }}>{visibleGroups.map((group) => <div key={group.label}><div className="muted" style={{ fontSize: 12, fontWeight: 600, marginBottom: 10, textTransform: "uppercase" }}>{group.label}</div><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{group.items.map(({ icon: Icon, label, count, onClick }) => <button key={label} type="button" onClick={onClick} className="rounded-lg border bg-card p-4 text-left transition-colors hover:bg-accent" style={{ display: "flex", alignItems: "center", gap: 12 }}><div style={{ width: 36, height: 36, borderRadius: 8, background: "var(--brand-soft)", color: "var(--brand)", display: "flex", alignItems: "center", justifyContent: "center" }}><Icon size={18} /></div><div><div className="text-sm font-medium">{label}</div><div className="text-xs text-muted-foreground">{Number(count || 0).toLocaleString()} records</div></div></button>)}</div></div>)}</div></div>;
}
