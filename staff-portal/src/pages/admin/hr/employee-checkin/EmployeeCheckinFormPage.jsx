import{useEffect,useState}from"react";import{useNavigate,useParams}from"react-router-dom";import toast from"react-hot-toast";import{PageHeader}from"@/components/shared/OriginalPrimitives";import EmployeeCheckinForm from"./components/EmployeeCheckinForm";import{createEmployeeCheckin,getEmployeeCheckin,updateEmployeeCheckin}from"@/services/hr/employeeCheckinService";import{getErrorMessage}from"@/utils/errors";export default function EmployeeCheckinFormPage(){const nav=useNavigate(),{id}=useParams(),editing=!!id,name=id?decodeURIComponent(id):"";const[doc,setDoc]=useState(null),[loading,setLoading]=useState(editing);useEffect(()=>{if(editing)getEmployeeCheckin(name).then(setDoc).catch(e=>toast.error(getErrorMessage(e))).finally(()=>setLoading(false))},[editing,name]);async function save(data){try{const r=editing?await updateEmployeeCheckin(name,data):await createEmployeeCheckin(data);toast.success(editing?"Employee Checkin updated":"Employee Checkin created");nav(`/dashboard/employee-checkins/${encodeURIComponent(r.name||name)}`)}catch(e){toast.error(getErrorMessage(e))}}if(loading)return <div className="muted">Loading employee checkin…</div>;if(editing&&doc&&!doc.can_edit)return <><PageHeader eyebrow="HR · Attendance" title="Employee Checkin"/><div className="panel p-5">You do not have permission to edit this employee checkin.</div></>;return <><PageHeader eyebrow="HR · Attendance" title={editing?"Edit Employee Checkin":"Create Employee Checkin"}/><div className="panel"><EmployeeCheckinForm checkin={doc} onSave={save}/></div></>}
