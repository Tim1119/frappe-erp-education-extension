import{useEffect,useState}from"react";import{useNavigate,useParams}from"react-router-dom";import toast from"react-hot-toast";import{PageHeader}from"@/components/shared/OriginalPrimitives";import AttendanceForm from"./components/AttendanceForm";import{createAttendance,getAttendance,updateAttendance}from"@/services/hr/attendanceService";import{getErrorMessage}from"@/utils/errors";export default function AttendanceFormPage(){const nav=useNavigate(),{id}=useParams(),editing=!!id,name=id?decodeURIComponent(id):"";const[doc,setDoc]=useState(null),[loading,setLoading]=useState(editing);useEffect(()=>{if(editing)getAttendance(name).then(setDoc).catch(e=>toast.error(getErrorMessage(e))).finally(()=>setLoading(false))},[editing,name]);async function save(data){try{const r=editing?await updateAttendance(name,data):await createAttendance(data);toast.success(editing?"Attendance updated":"Attendance created");nav(`/dashboard/attendance/${encodeURIComponent(r.name||name)}`)}catch(e){toast.error(getErrorMessage(e))}}if(loading)return <div className="muted">Loading attendance…</div>;if(editing&&doc&&!doc.can_edit)return <><PageHeader eyebrow="HR · Attendance" title="Attendance"/><div className="panel p-5">Only draft attendance can be edited.</div></>;return <><PageHeader eyebrow="HR · Attendance" title={editing?"Edit Attendance":"Create Attendance"}/><div className="panel"><AttendanceForm attendance={doc} onSave={save}/></div></>}
