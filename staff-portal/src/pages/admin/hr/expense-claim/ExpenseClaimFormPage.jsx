import{useEffect,useState}from"react";import{useNavigate,useParams}from"react-router-dom";import toast from"react-hot-toast";import{PageHeader}from"@/components/shared/OriginalPrimitives";import ExpenseClaimForm from"./components/ExpenseClaimForm";import{createExpenseClaim,getExpenseClaim,updateExpenseClaim}from"@/services/hr/expenseClaimService";import{getErrorMessage}from"@/utils/errors";export default function ExpenseClaimFormPage(){const n=useNavigate(),{id}=useParams(),edit=!!id,name=id?decodeURIComponent(id):"",[d,setD]=useState(null);useEffect(()=>{if(edit)getExpenseClaim(name).then(setD)},[edit,name]);async function save(x){try{const r=edit?await updateExpenseClaim(name,x):await createExpenseClaim(x);toast.success(`Expense Claim ${edit?"updated":"created"}`);n(`/dashboard/expense-claims/${encodeURIComponent(r.name)}`)}catch(e){toast.error(getErrorMessage(e))}}return <><PageHeader eyebrow="HR · Expense Claim" title={`${edit?"Edit":"Create"} Expense Claim`}/><div className="panel"><ExpenseClaimForm doc={d} onSave={save}/></div></>}
