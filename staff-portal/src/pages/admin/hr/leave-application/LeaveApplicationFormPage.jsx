import{useEffect,useState}from"react";import{useNavigate,useParams}from"react-router-dom";import toast from"react-hot-toast";import{PageHeader}from"@/components/shared/OriginalPrimitives";import LeaveApplicationForm from"./components/LeaveApplicationForm";import{createLeaveApplication,getLeaveApplication,updateLeaveApplication}from"@/services/hr/leaveApplicationService";import{getErrorMessage}from"@/utils/errors";export default function LeaveApplicationFormPage(){const nav=useNavigate(),{id}=useParams(),editing=!!id,name=id?decodeURIComponent(id):"";const[doc,setDoc]=useState(null),[loading,setLoading]=useState(editing);useEffect(()=>{if(editing)getLeaveApplication(name).then(setDoc).catch(e=>toast.error(getErrorMessage(e))).finally(()=>setLoading(false))},[editing,name]);async function save(data){try{const r=editing?await updateLeaveApplication(name,data):await createLeaveApplication(data);toast.success(editing?"Leave Application updated":"Leave Application created");nav(`/dashboard/leave-applications/${encodeURIComponent(r.name||name)}`)}catch(e){toast.error(getErrorMessage(e))}}if(loading)return <div className="muted">Loading leave application…</div>;if(editing&&doc&&!doc.can_edit)return <><PageHeader eyebrow="HR · Leaves" title="Leave Application"/><div className="panel p-5">Only draft leave applications can be edited.</div></>;return <><PageHeader eyebrow="HR · Leaves" title={editing?"Edit Leave Application":"Create Leave Application"}/><div className="panel"><LeaveApplicationForm application={doc} onSave={save}/></div></>}
