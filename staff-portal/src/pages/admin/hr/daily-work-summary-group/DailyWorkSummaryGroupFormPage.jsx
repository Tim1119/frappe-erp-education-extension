import{useEffect,useState}from"react";import{useNavigate,useParams}from"react-router-dom";import toast from"react-hot-toast";import{PageHeader}from"@/components/shared/OriginalPrimitives";import DailyWorkSummaryGroupForm from"./components/DailyWorkSummaryGroupForm";import{createDailyWorkSummaryGroup,getDailyWorkSummaryGroup,updateDailyWorkSummaryGroup}from"@/services/hr/dailyWorkSummaryGroupService";import{getErrorMessage}from"@/utils/errors";export default function DailyWorkSummaryGroupFormPage(){const nav=useNavigate(),{id}=useParams(),editing=!!id,name=id?decodeURIComponent(id):"";const[doc,setDoc]=useState(null),[loading,setLoading]=useState(editing);useEffect(()=>{if(editing)getDailyWorkSummaryGroup(name).then(setDoc).catch(e=>toast.error(getErrorMessage(e))).finally(()=>setLoading(false))},[editing,name]);async function save(data){try{const r=editing?await updateDailyWorkSummaryGroup(name,data):await createDailyWorkSummaryGroup(data);toast.success(editing?"Daily Work Summary Group updated":"Daily Work Summary Group created");nav(`/dashboard/daily-work-summary-groups/${encodeURIComponent(r.name||name)}`)}catch(e){toast.error(getErrorMessage(e))}}if(loading)return <div className="muted">Loading group…</div>;if(editing&&doc&&!doc.can_edit)return <><PageHeader eyebrow="HR · Settings" title="Daily Work Summary Group"/><div className="panel p-5">You do not have permission to edit this group.</div></>;return <><PageHeader eyebrow="HR · Settings" title={editing?"Edit Daily Work Summary Group":"Create Daily Work Summary Group"}/><div className="panel"><DailyWorkSummaryGroupForm group={doc} onSave={save}/></div></>}
