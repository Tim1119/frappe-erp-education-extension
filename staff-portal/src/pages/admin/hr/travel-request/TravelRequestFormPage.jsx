import{useEffect,useState}from"react";import{useNavigate,useParams}from"react-router-dom";import toast from"react-hot-toast";import{PageHeader}from"@/components/shared/OriginalPrimitives";import TravelRequestForm from"./components/TravelRequestForm";import{createTravelRequest,getTravelRequest,updateTravelRequest}from"@/services/hr/travelRequestService";import{getErrorMessage}from"@/utils/errors";export default function TravelRequestFormPage(){const n=useNavigate(),{id}=useParams(),edit=!!id,name=id?decodeURIComponent(id):"",[d,setD]=useState(null);useEffect(()=>{if(edit)getTravelRequest(name).then(setD)},[edit,name]);async function save(x){try{const r=edit?await updateTravelRequest(name,x):await createTravelRequest(x);toast.success(`Travel Request ${edit?"updated":"created"}`);n(`/dashboard/travel-requests/${encodeURIComponent(r.name)}`)}catch(e){toast.error(getErrorMessage(e))}}return <><PageHeader eyebrow="HR · Travel Request" title={`${edit?"Edit":"Create"} Travel Request`}/><div className="panel"><TravelRequestForm doc={d} onSave={save}/></div></>}
