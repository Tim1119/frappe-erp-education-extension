import{useEffect,useState}from"react";import{useNavigate,useParams}from"react-router-dom";import toast from"react-hot-toast";import{PageHeader}from"@/components/shared/OriginalPrimitives";import CompensatoryLeaveRequestForm from"./components/CompensatoryLeaveRequestForm";import{createCompensatoryLeaveRequest,getCompensatoryLeaveRequest,updateCompensatoryLeaveRequest}from"@/services/hr/leaves/compensatoryLeaveRequestService";import{getErrorMessage}from"@/utils/errors";export default function CompensatoryLeaveRequestFormPage(){const nav=useNavigate(),{id}=useParams(),editing=!!id,name=id?decodeURIComponent(id):"";const[doc,setDoc]=useState(null),[loading,setLoading]=useState(editing);useEffect(()=>{if(editing)getCompensatoryLeaveRequest(name).then(setDoc).catch(e=>toast.error(getErrorMessage(e))).finally(()=>setLoading(false))},[editing,name]);async function save(data){try{const r=editing?await updateCompensatoryLeaveRequest(name,data):await createCompensatoryLeaveRequest(data);toast.success(editing?"Request updated":"Request created");nav(`/dashboard/compensatory-leave-requests/${encodeURIComponent(r.name||name)}`)}catch(e){toast.error(getErrorMessage(e))}}if(loading)return <div className="muted">Loading request…</div>;if(editing&&doc&&!doc.can_edit)return <><PageHeader eyebrow="HR · Leaves" title="Compensatory Leave Request"/><div className="panel p-5">Only draft requests can be edited.</div></>;return <><PageHeader eyebrow="HR · Leaves" title={editing?"Edit Compensatory Leave Request":"Create Compensatory Leave Request"}/><div className="panel"><CompensatoryLeaveRequestForm request={doc} onSave={save}/></div></>}
