import { useEffect, useState } from "react";
import { useNavigate } from "react-router-dom";
import { Plus, UsersRound } from "lucide-react";
import toast from "react-hot-toast";
import { EmptyState, PageHeader } from "@/components/shared/OriginalPrimitives"; import Toolbar from "@/components/shared/Toolbar"; import Pager from "@/components/shared/Pager"; import ConfirmDialog from "@/components/shared/ConfirmDialog"; import RowActionsMenu from "@/components/shared/RowActionsMenu"; import { usePagination } from "@/hooks";
import { deleteEmployeeGroup, getEmployeeGroups } from "@/services/hr/employeeGroupService"; import { getErrorMessage } from "@/utils/errors";

export default function EmployeeGroupsPage() { const navigate = useNavigate(); const { page, setPage } = usePagination(1); const [rows, setRows] = useState([]); const [count, setCount] = useState(0); const [search, setSearch] = useState(""); const [loading, setLoading] = useState(true); const [target, setTarget] = useState(null); async function load() { try { setLoading(true); const result = await getEmployeeGroups({ page, search }); setRows(result.rows || []); setCount(result.count || 0); } catch (error) { toast.error(getErrorMessage(error)); } finally { setLoading(false); } } useEffect(() => { load(); /* eslint-disable-next-line react-hooks/exhaustive-deps */ }, [page, search]); async function remove() { try { await deleteEmployeeGroup(target.name); toast.success("Employee Group deleted"); setTarget(null); load(); } catch (error) { toast.error(getErrorMessage(error)); } } return <><PageHeader eyebrow="HR" title="Employee Groups" sub={loading ? "Loading..." : `${count} employee groups`} button={<button className="btn btn-primary" onClick={() => navigate("/dashboard/employee-groups/new")}><Plus size={15} /> Add Employee Group</button>} /><Toolbar search={search} onSearch={(value) => { setSearch(value); setPage(1); }} /><div className="panel"><div style={{ overflowX: "auto" }}><table className="tbl"><thead><tr><th>Employee Group</th><th>Members</th><th /></tr></thead><tbody>{rows.map((row) => <tr key={row.name} className="cursor-pointer" onClick={() => navigate(`/dashboard/employee-groups/${encodeURIComponent(row.name)}`)}><td><div style={{ display: "flex", alignItems: "center", gap: 9 }}><UsersRound size={17} style={{ color: "var(--ink-4)" }} />{row.employee_group_name || row.name}</div></td><td>{Number(row.member_count || 0).toLocaleString()}</td><td onClick={(event) => event.stopPropagation()}><RowActionsMenu onView={() => navigate(`/dashboard/employee-groups/${encodeURIComponent(row.name)}`)} onEdit={row.can_edit ? () => navigate(`/dashboard/employee-groups/${encodeURIComponent(row.name)}/edit`) : undefined} onDelete={() => setTarget(row)} /></td></tr>)}{!loading && !rows.length && <tr><td colSpan={3}><EmptyState title="No employee groups found" sub="No employee group records available." /></td></tr>}</tbody></table></div><Pager page={page} setPage={setPage} pageSize={20} count={count} /></div><ConfirmDialog open={Boolean(target)} onClose={() => setTarget(null)} onConfirm={remove} title={`Delete ${target?.employee_group_name || target?.name}?`} description="This action cannot be undone." confirmLabel="Delete" /></>; }
