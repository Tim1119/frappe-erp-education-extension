# Used only by `npm run dev` (vite.config.js proxies /api requests to this host).
# Point it at your local/staging Frappe site. Not needed in production if this
# app is served from the same origin as the Frappe site.
VITE_API_BASE_URL=http://127.0.0.1:8000
